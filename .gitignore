.DS_Store

# Zone de proposition — dérivés locaux (CR, rendus, faits calculés).
# Le dérivé n'est jamais le saisi : ces fichiers ne rejoignent jamais le canon.
# Le dossier entier est ignoré (couvre aussi les rendus *.html).
zone-proposition/
