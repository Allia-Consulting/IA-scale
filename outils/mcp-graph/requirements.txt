# Dépendances du serveur MCP Graph (outils/mcp-graph/server.py).
# Installation : python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
mcp>=1.2.0
httpx>=0.27.0
azure-identity>=1.16.0
